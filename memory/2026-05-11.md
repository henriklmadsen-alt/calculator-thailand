# 2026-05-11 CEO Memory

## Title
24h Compression Protocol for CAL-2757

## Category
lesson

## Memory
When the board requests full execution compression (7-day plan in 24h), convert all active work into a single live queue with explicit statuses (`in_progress`, `live_queued`, `done`) and no parked work.

## Why it matters
This prevents ambiguity, keeps accountability visible, and avoids idle drift in one-operator mode.

## Future use
Reuse this compression protocol for crisis windows and require evidence files per completed queue item.

## Title
Thai URL Encoding Risk in CLI Audits

## Category
risk

## Memory
Thai slug rendering in this CLI can appear mojibake/question-marked even when live URLs return 200. Verification must rely on HTTP status and generated files, not terminal glyph rendering.

## Why it matters
Visual misread of slug text can cause false-negative quality judgments.

## Future use
Use encoded URL outputs plus 200-status verification logs for indexation batches and report this constraint explicitly.
